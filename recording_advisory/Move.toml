[package]
name = "recording_advisory"
edition = "2024"
license = "Apache 2.0"
authors = ["Miso Labs, Inc. (build@miso.network)"]

# One concern, one package: the parental advisory rating and nothing else. It
# needs only musicos core, to operate on `Recording` through its cap-gated
# `uid_mut`.
[dependencies]
musicos = { git = "https://github.com/misofm/musicos.git", rev = "f7a2be77ecc4429a1c5ff70800490b1946b1defb" }
