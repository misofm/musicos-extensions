[package]
name = "recording_advisory"
edition = "2024"
license = "Apache 2.0"
authors = ["Miso Labs, Inc. (build@miso.network)"]

# One concern, one package: the parental advisory rating and nothing else. It
# needs only miso core, to operate on `Recording` through its cap-gated
# `uid_mut`.
[dependencies]
miso = { git = "https://github.com/misofm/protocol.git", rev = "09f0dc699a112c37d8da8a765596cc1ed623fe79" }
