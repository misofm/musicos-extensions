[package]
name = "release_description"
edition = "2024"
license = "Apache 2.0"
authors = ["Miso Labs, Inc. (build@miso.network)"]

# Free-form prose with no vocabulary to enforce and nothing to validate against,
# so there is nothing to depend on beyond musicos core — needed only to reach
# `Release` through its cap-gated `uid_mut`.
[dependencies]
musicos = { git = "https://github.com/misofm/musicos.git", rev = "b3d5d4005fe36044d90e318f864dbc8b01de41da" }
