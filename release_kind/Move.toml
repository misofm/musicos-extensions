[package]
name = "release_kind"
edition = "2024"
license = "Apache 2.0"
authors = ["Miso Labs, Inc. (build@miso.network)"]

# Self-asserted, with no vocabulary to enforce — hence no dependency beyond musicos
# core, which is only needed to reach `Release` through its cap-gated `uid_mut`.
[dependencies]
musicos = { git = "https://github.com/misofm/musicos.git", rev = "7f67ecc7b95f585346a9d52546582bea28b64e96" }
