[package]
name = "release_kind"
edition = "2024"
license = "Apache 2.0"
authors = ["Miso Labs, Inc. (build@miso.network)"]

# Self-asserted, with no vocabulary to enforce — hence no dependency beyond miso
# core, which is only needed to reach `Release` through its cap-gated `uid_mut`.
[dependencies]
miso = { git = "https://github.com/misonetwork/protocol.git", rev = "7bda0bb740c32a75ef76c0739cb671b3de77d338" }
