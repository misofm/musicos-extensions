[package]
name = "composition_title"
edition = "2024"
license = "Apache 2.0"
authors = ["Miso Labs, Inc. (build@miso.network)"]

# A free string with no vocabulary to enforce and nothing to validate against,
# so there is nothing to depend on beyond musicos core — needed only to reach
# `Composition` through its cap-gated `uid_mut`. Pinned at current core, which
# stores no title.
[dependencies]
musicos = { git = "https://github.com/misofm/musicos.git", rev = "6dff4deca5ced186989c064e152c92a06384750c" }
