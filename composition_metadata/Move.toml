[package]
name = "composition_metadata"
edition = "2024"
license = "Apache 2.0"
authors = ["Miso Labs, Inc. (build@miso.network)"]

# Descriptive metadata with no vocabulary to enforce and nothing to validate
# against, so there is nothing to depend on beyond musicos core — needed only
# to reach `Composition` through its cap-gated `uid_mut`. Pinned at the
# revision that removed the title from core.
[dependencies]
musicos = { git = "https://github.com/misofm/musicos.git", rev = "676dbd801583761b7e5ab53b4476e8606a12a8d7" }
